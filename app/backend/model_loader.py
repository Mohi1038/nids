# # from logistic_model import LogisticRegression  # Must be imported before unpickling
# # import pickle
# from main import model 
# import numpy as np

# def classify_packet(features):
#     features = np.array([list(features.values())])  # convert dict to feature vector
#     pred = model.predict(features)
#     return "Anomalous" if pred[0] == 1 else "Normal"
